# Cloud Mapping

Shared cloud mapping plugin for workbook-to-CSV discovery, LocalEvomap-assisted reranking, and contribution evaluation.

- Repo source: `plugins/cloud_mapping/`
- Standalone deploy target: `~/cloud-mapping`
- Runtime artifacts stay out of git and default under `plugins/cloud_mapping/data/` and `plugins/cloud_mapping/reports/`
